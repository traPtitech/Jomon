// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/traPtitech/Jomon/internal/ent/user"
	"github.com/traPtitech/Jomon/internal/ent/usersubject"
)

// UserSubject is the model entity for the UserSubject schema.
type UserSubject struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID uuid.UUID `json:"user_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserSubjectQuery when eager-loading is set.
	Edges        UserSubjectEdges `json:"edges"`
	selectValues sql.SelectValues
}

// UserSubjectEdges holds the relations/edges for other nodes in the graph.
type UserSubjectEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserSubjectEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserSubject) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case usersubject.FieldID:
			values[i] = new(sql.NullString)
		case usersubject.FieldUserID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserSubject fields.
func (_m *UserSubject) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case usersubject.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case usersubject.FieldUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value != nil {
				_m.UserID = *value
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserSubject.
// This includes values selected through modifiers, order, etc.
func (_m *UserSubject) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserSubject entity.
func (_m *UserSubject) QueryUser() *UserQuery {
	return NewUserSubjectClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this UserSubject.
// Note that you need to call UserSubject.Unwrap() before calling this method if this UserSubject
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *UserSubject) Update() *UserSubjectUpdateOne {
	return NewUserSubjectClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the UserSubject entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *UserSubject) Unwrap() *UserSubject {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserSubject is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *UserSubject) String() string {
	var builder strings.Builder
	builder.WriteString("UserSubject(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserID))
	builder.WriteByte(')')
	return builder.String()
}

// UserSubjects is a parsable slice of UserSubject.
type UserSubjects []*UserSubject
