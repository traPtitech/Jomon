// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/traPtitech/Jomon/ent/application"
	"github.com/traPtitech/Jomon/ent/applicationstatus"
	"github.com/traPtitech/Jomon/ent/user"
)

// ApplicationStatus is the model entity for the ApplicationStatus schema.
type ApplicationStatus struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Status holds the value of the "status" field.
	Status applicationstatus.Status `json:"status,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ApplicationStatusQuery when eager-loading is set.
	Edges                   ApplicationStatusEdges `json:"edges"`
	application_status      *uuid.UUID
	application_status_user *uuid.UUID
	selectValues            sql.SelectValues
}

// ApplicationStatusEdges holds the relations/edges for other nodes in the graph.
type ApplicationStatusEdges struct {
	// Application holds the value of the application edge.
	Application *Application `json:"application,omitempty"`
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ApplicationOrErr returns the Application value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ApplicationStatusEdges) ApplicationOrErr() (*Application, error) {
	if e.Application != nil {
		return e.Application, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: application.Label}
	}
	return nil, &NotLoadedError{edge: "application"}
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ApplicationStatusEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ApplicationStatus) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case applicationstatus.FieldStatus:
			values[i] = new(sql.NullString)
		case applicationstatus.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case applicationstatus.FieldID:
			values[i] = new(uuid.UUID)
		case applicationstatus.ForeignKeys[0]: // application_status
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case applicationstatus.ForeignKeys[1]: // application_status_user
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ApplicationStatus fields.
func (_m *ApplicationStatus) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case applicationstatus.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case applicationstatus.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = applicationstatus.Status(value.String)
			}
		case applicationstatus.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case applicationstatus.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field application_status", values[i])
			} else if value.Valid {
				_m.application_status = new(uuid.UUID)
				*_m.application_status = *value.S.(*uuid.UUID)
			}
		case applicationstatus.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field application_status_user", values[i])
			} else if value.Valid {
				_m.application_status_user = new(uuid.UUID)
				*_m.application_status_user = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ApplicationStatus.
// This includes values selected through modifiers, order, etc.
func (_m *ApplicationStatus) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryApplication queries the "application" edge of the ApplicationStatus entity.
func (_m *ApplicationStatus) QueryApplication() *ApplicationQuery {
	return NewApplicationStatusClient(_m.config).QueryApplication(_m)
}

// QueryUser queries the "user" edge of the ApplicationStatus entity.
func (_m *ApplicationStatus) QueryUser() *UserQuery {
	return NewApplicationStatusClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this ApplicationStatus.
// Note that you need to call ApplicationStatus.Unwrap() before calling this method if this ApplicationStatus
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ApplicationStatus) Update() *ApplicationStatusUpdateOne {
	return NewApplicationStatusClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ApplicationStatus entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ApplicationStatus) Unwrap() *ApplicationStatus {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ApplicationStatus is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ApplicationStatus) String() string {
	var builder strings.Builder
	builder.WriteString("ApplicationStatus(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ApplicationStatusSlice is a parsable slice of ApplicationStatus.
type ApplicationStatusSlice []*ApplicationStatus
