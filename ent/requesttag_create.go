// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/traPtitech/Jomon/ent/request"
	"github.com/traPtitech/Jomon/ent/requesttag"
	"github.com/traPtitech/Jomon/ent/tag"
)

// RequestTagCreate is the builder for creating a RequestTag entity.
type RequestTagCreate struct {
	config
	mutation *RequestTagMutation
	hooks    []Hook
}

// SetCreatedAt sets the "created_at" field.
func (rtc *RequestTagCreate) SetCreatedAt(t time.Time) *RequestTagCreate {
	rtc.mutation.SetCreatedAt(t)
	return rtc
}

// SetNillableCreatedAt sets the "created_at" field if the given value is not nil.
func (rtc *RequestTagCreate) SetNillableCreatedAt(t *time.Time) *RequestTagCreate {
	if t != nil {
		rtc.SetCreatedAt(*t)
	}
	return rtc
}

// SetRequestID sets the "request" edge to the Request entity by ID.
func (rtc *RequestTagCreate) SetRequestID(id int) *RequestTagCreate {
	rtc.mutation.SetRequestID(id)
	return rtc
}

// SetRequest sets the "request" edge to the Request entity.
func (rtc *RequestTagCreate) SetRequest(r *Request) *RequestTagCreate {
	return rtc.SetRequestID(r.ID)
}

// SetTagID sets the "tag" edge to the Tag entity by ID.
func (rtc *RequestTagCreate) SetTagID(id int) *RequestTagCreate {
	rtc.mutation.SetTagID(id)
	return rtc
}

// SetTag sets the "tag" edge to the Tag entity.
func (rtc *RequestTagCreate) SetTag(t *Tag) *RequestTagCreate {
	return rtc.SetTagID(t.ID)
}

// Mutation returns the RequestTagMutation object of the builder.
func (rtc *RequestTagCreate) Mutation() *RequestTagMutation {
	return rtc.mutation
}

// Save creates the RequestTag in the database.
func (rtc *RequestTagCreate) Save(ctx context.Context) (*RequestTag, error) {
	var (
		err  error
		node *RequestTag
	)
	rtc.defaults()
	if len(rtc.hooks) == 0 {
		if err = rtc.check(); err != nil {
			return nil, err
		}
		node, err = rtc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*RequestTagMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = rtc.check(); err != nil {
				return nil, err
			}
			rtc.mutation = mutation
			node, err = rtc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(rtc.hooks) - 1; i >= 0; i-- {
			mut = rtc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, rtc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (rtc *RequestTagCreate) SaveX(ctx context.Context) *RequestTag {
	v, err := rtc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// defaults sets the default values of the builder before save.
func (rtc *RequestTagCreate) defaults() {
	if _, ok := rtc.mutation.CreatedAt(); !ok {
		v := requesttag.DefaultCreatedAt()
		rtc.mutation.SetCreatedAt(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (rtc *RequestTagCreate) check() error {
	if _, ok := rtc.mutation.CreatedAt(); !ok {
		return &ValidationError{Name: "created_at", err: errors.New("ent: missing required field \"created_at\"")}
	}
	if _, ok := rtc.mutation.RequestID(); !ok {
		return &ValidationError{Name: "request", err: errors.New("ent: missing required edge \"request\"")}
	}
	if _, ok := rtc.mutation.TagID(); !ok {
		return &ValidationError{Name: "tag", err: errors.New("ent: missing required edge \"tag\"")}
	}
	return nil
}

func (rtc *RequestTagCreate) sqlSave(ctx context.Context) (*RequestTag, error) {
	_node, _spec := rtc.createSpec()
	if err := sqlgraph.CreateNode(ctx, rtc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (rtc *RequestTagCreate) createSpec() (*RequestTag, *sqlgraph.CreateSpec) {
	var (
		_node = &RequestTag{config: rtc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: requesttag.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: requesttag.FieldID,
			},
		}
	)
	if value, ok := rtc.mutation.CreatedAt(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: requesttag.FieldCreatedAt,
		})
		_node.CreatedAt = value
	}
	if nodes := rtc.mutation.RequestIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   requesttag.RequestTable,
			Columns: []string{requesttag.RequestColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: request.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.request_tag = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := rtc.mutation.TagIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   requesttag.TagTable,
			Columns: []string{requesttag.TagColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: tag.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.tag_request_tag = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// RequestTagCreateBulk is the builder for creating many RequestTag entities in bulk.
type RequestTagCreateBulk struct {
	config
	builders []*RequestTagCreate
}

// Save creates the RequestTag entities in the database.
func (rtcb *RequestTagCreateBulk) Save(ctx context.Context) ([]*RequestTag, error) {
	specs := make([]*sqlgraph.CreateSpec, len(rtcb.builders))
	nodes := make([]*RequestTag, len(rtcb.builders))
	mutators := make([]Mutator, len(rtcb.builders))
	for i := range rtcb.builders {
		func(i int, root context.Context) {
			builder := rtcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*RequestTagMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, rtcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, rtcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, rtcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (rtcb *RequestTagCreateBulk) SaveX(ctx context.Context) []*RequestTag {
	v, err := rtcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
