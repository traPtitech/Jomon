// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/traPtitech/Jomon/ent/administrator"
)

// AdministratorCreate is the builder for creating a Administrator entity.
type AdministratorCreate struct {
	config
	mutation *AdministratorMutation
	hooks    []Hook
}

// SetTrapID sets the "trap_id" field.
func (ac *AdministratorCreate) SetTrapID(s string) *AdministratorCreate {
	ac.mutation.SetTrapID(s)
	return ac
}

// Mutation returns the AdministratorMutation object of the builder.
func (ac *AdministratorCreate) Mutation() *AdministratorMutation {
	return ac.mutation
}

// Save creates the Administrator in the database.
func (ac *AdministratorCreate) Save(ctx context.Context) (*Administrator, error) {
	var (
		err  error
		node *Administrator
	)
	if len(ac.hooks) == 0 {
		if err = ac.check(); err != nil {
			return nil, err
		}
		node, err = ac.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*AdministratorMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = ac.check(); err != nil {
				return nil, err
			}
			ac.mutation = mutation
			node, err = ac.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(ac.hooks) - 1; i >= 0; i-- {
			mut = ac.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, ac.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (ac *AdministratorCreate) SaveX(ctx context.Context) *Administrator {
	v, err := ac.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (ac *AdministratorCreate) check() error {
	if _, ok := ac.mutation.TrapID(); !ok {
		return &ValidationError{Name: "trap_id", err: errors.New("ent: missing required field \"trap_id\"")}
	}
	return nil
}

func (ac *AdministratorCreate) sqlSave(ctx context.Context) (*Administrator, error) {
	_node, _spec := ac.createSpec()
	if err := sqlgraph.CreateNode(ctx, ac.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (ac *AdministratorCreate) createSpec() (*Administrator, *sqlgraph.CreateSpec) {
	var (
		_node = &Administrator{config: ac.config}
		_spec = &sqlgraph.CreateSpec{
			Table: administrator.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: administrator.FieldID,
			},
		}
	)
	if value, ok := ac.mutation.TrapID(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: administrator.FieldTrapID,
		})
		_node.TrapID = value
	}
	return _node, _spec
}

// AdministratorCreateBulk is the builder for creating many Administrator entities in bulk.
type AdministratorCreateBulk struct {
	config
	builders []*AdministratorCreate
}

// Save creates the Administrator entities in the database.
func (acb *AdministratorCreateBulk) Save(ctx context.Context) ([]*Administrator, error) {
	specs := make([]*sqlgraph.CreateSpec, len(acb.builders))
	nodes := make([]*Administrator, len(acb.builders))
	mutators := make([]Mutator, len(acb.builders))
	for i := range acb.builders {
		func(i int, root context.Context) {
			builder := acb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*AdministratorMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, acb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, acb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, acb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (acb *AdministratorCreateBulk) SaveX(ctx context.Context) []*Administrator {
	v, err := acb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
