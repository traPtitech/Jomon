// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/traPtitech/Jomon/ent/group"
	"github.com/traPtitech/Jomon/ent/groupowner"
)

// GroupOwnerCreate is the builder for creating a GroupOwner entity.
type GroupOwnerCreate struct {
	config
	mutation *GroupOwnerMutation
	hooks    []Hook
}

// SetOwner sets the "owner" field.
func (goc *GroupOwnerCreate) SetOwner(s string) *GroupOwnerCreate {
	goc.mutation.SetOwner(s)
	return goc
}

// SetCreatedAt sets the "created_at" field.
func (goc *GroupOwnerCreate) SetCreatedAt(t time.Time) *GroupOwnerCreate {
	goc.mutation.SetCreatedAt(t)
	return goc
}

// SetNillableCreatedAt sets the "created_at" field if the given value is not nil.
func (goc *GroupOwnerCreate) SetNillableCreatedAt(t *time.Time) *GroupOwnerCreate {
	if t != nil {
		goc.SetCreatedAt(*t)
	}
	return goc
}

// SetID sets the "id" field.
func (goc *GroupOwnerCreate) SetID(u uuid.UUID) *GroupOwnerCreate {
	goc.mutation.SetID(u)
	return goc
}

// SetGroupID sets the "group" edge to the Group entity by ID.
func (goc *GroupOwnerCreate) SetGroupID(id uuid.UUID) *GroupOwnerCreate {
	goc.mutation.SetGroupID(id)
	return goc
}

// SetGroup sets the "group" edge to the Group entity.
func (goc *GroupOwnerCreate) SetGroup(g *Group) *GroupOwnerCreate {
	return goc.SetGroupID(g.ID)
}

// Mutation returns the GroupOwnerMutation object of the builder.
func (goc *GroupOwnerCreate) Mutation() *GroupOwnerMutation {
	return goc.mutation
}

// Save creates the GroupOwner in the database.
func (goc *GroupOwnerCreate) Save(ctx context.Context) (*GroupOwner, error) {
	var (
		err  error
		node *GroupOwner
	)
	goc.defaults()
	if len(goc.hooks) == 0 {
		if err = goc.check(); err != nil {
			return nil, err
		}
		node, err = goc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*GroupOwnerMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = goc.check(); err != nil {
				return nil, err
			}
			goc.mutation = mutation
			node, err = goc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(goc.hooks) - 1; i >= 0; i-- {
			mut = goc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, goc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (goc *GroupOwnerCreate) SaveX(ctx context.Context) *GroupOwner {
	v, err := goc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// defaults sets the default values of the builder before save.
func (goc *GroupOwnerCreate) defaults() {
	if _, ok := goc.mutation.CreatedAt(); !ok {
		v := groupowner.DefaultCreatedAt()
		goc.mutation.SetCreatedAt(v)
	}
	if _, ok := goc.mutation.ID(); !ok {
		v := groupowner.DefaultID()
		goc.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (goc *GroupOwnerCreate) check() error {
	if _, ok := goc.mutation.Owner(); !ok {
		return &ValidationError{Name: "owner", err: errors.New("ent: missing required field \"owner\"")}
	}
	if _, ok := goc.mutation.CreatedAt(); !ok {
		return &ValidationError{Name: "created_at", err: errors.New("ent: missing required field \"created_at\"")}
	}
	if _, ok := goc.mutation.GroupID(); !ok {
		return &ValidationError{Name: "group", err: errors.New("ent: missing required edge \"group\"")}
	}
	return nil
}

func (goc *GroupOwnerCreate) sqlSave(ctx context.Context) (*GroupOwner, error) {
	_node, _spec := goc.createSpec()
	if err := sqlgraph.CreateNode(ctx, goc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	return _node, nil
}

func (goc *GroupOwnerCreate) createSpec() (*GroupOwner, *sqlgraph.CreateSpec) {
	var (
		_node = &GroupOwner{config: goc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: groupowner.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: groupowner.FieldID,
			},
		}
	)
	if id, ok := goc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := goc.mutation.Owner(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: groupowner.FieldOwner,
		})
		_node.Owner = value
	}
	if value, ok := goc.mutation.CreatedAt(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: groupowner.FieldCreatedAt,
		})
		_node.CreatedAt = value
	}
	if nodes := goc.mutation.GroupIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   groupowner.GroupTable,
			Columns: []string{groupowner.GroupColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeUUID,
					Column: group.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.group_owner = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// GroupOwnerCreateBulk is the builder for creating many GroupOwner entities in bulk.
type GroupOwnerCreateBulk struct {
	config
	builders []*GroupOwnerCreate
}

// Save creates the GroupOwner entities in the database.
func (gocb *GroupOwnerCreateBulk) Save(ctx context.Context) ([]*GroupOwner, error) {
	specs := make([]*sqlgraph.CreateSpec, len(gocb.builders))
	nodes := make([]*GroupOwner, len(gocb.builders))
	mutators := make([]Mutator, len(gocb.builders))
	for i := range gocb.builders {
		func(i int, root context.Context) {
			builder := gocb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*GroupOwnerMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, gocb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, gocb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, gocb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (gocb *GroupOwnerCreateBulk) SaveX(ctx context.Context) []*GroupOwner {
	v, err := gocb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
